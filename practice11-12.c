#include<stdio.h>

int str_len(char *nptr){
    size_t str_len;

    while(*nptr!='\0'){
        str_len++;
    }
    return str_len;
}

int atoi(const char *nptr){

    while(*nptr!='\0'){

    }
}